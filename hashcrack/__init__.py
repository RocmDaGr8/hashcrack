"""HashCrack — a hash identifier and dictionary/rule-based cracker.

Identifies common hash types by format and runs offline wordlist attacks
(with optional rule-based mangling) against the hashlib algorithm family.
Built for CTFs, lab exercises, and password-strength auditing — run it only
against hashes you are authorized to test.
"""

__version__ = "1.0.0"
